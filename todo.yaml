---
TODOs:
  documentation:
    - Add a section guiding through the initial setup
    - Add usage and getting started section
    - Add more example
  code:
    Validate:
      - Metadata (resource name, namespace)
      - APIVersion
      - Kind
    Resources:
      - Config
      - Namespaces: e.g. allow configuring multiple directories
      - Documentation: |
          DocConfig can be used to set up a new directory containing
          notes or docs.
    Commands:
      - apply: |
          User can "CreateOrUpdate" many resources in batch.
          It requires the "-f" flag.
      - grep: |
          User can search resources content by regex.

