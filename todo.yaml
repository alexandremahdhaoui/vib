---
TODOs:
  documentation:
    - Add a section guiding through the initial setup
    - Add usage and getting started section
    - Add more example
  code:
    - 'Depend on the least amount of packages (hopefully only yamlv3 package)':
        - Remove `urfave/cli` -> use std library
    - refactor internal/
    - refactor cmd/
    - Implement APIServer

